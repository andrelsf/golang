# GO Password Generate

A simple password generator made in `Golang`.

Usage:
```python
Usage: go run gopg.go -a <amount> -s <size>
       -a : passwords amount.
       -s : passwords size.
golang \o/
```

